#' @param proj_years [Projection years][ageproR::projection_years]:
#' Input can be Sequence of years in from first to last year of
#' projection or the number of years in the time projection.
#' @param num_ages Number of Age classes
#' @param input_option Option to indicate this parameter will be:
#' \itemize{
#'  \item `0` By default, done interactively via interface.
#'  \item `1` Imported via location of an existing data file.
#' }
#' @param time_varying Logical flag that enables the stochastic parameter
#' to use as a time-varying array if TRUE (or 1). Otherwise, FALSE the
#' vector will cover "all years" of the projection. Default is TRUE.
#'

